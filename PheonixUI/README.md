# PheonixUI Documentation

To start the ui, ensure you are in the virtual env (gokart-env --> `source gokart-env/bin/activate`):
Then run:
```bash
python widget.py && wmctrl -r Widget -b add,fullscreen
```
